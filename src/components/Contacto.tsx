"use client";

import Image from "next/image";
import { useState } from "react";

export default function Contacto() {

    return (
        <section id="buyandship" className="relative w-full flex justify-center items-center py-20 bg-gradient-to-r from-blue-400 to to-blue-900 font-[Montserrat]">
            <div className="relative w-full max-w-4xl ">
                <div className="rounded-[28px] object-cover bg-blue-900">
                    <h1 className="font-extrabold text-[55px] text-white text-center absolute top-5 ">
                        Contáctanos
                    </h1>
                </div>
            </div>
        </section>
    )
}